# Environment templates:
#
#   Docker Compose (recommended): cp .env.docker.example .env
#   Local host-run:               cp .env.local.example .env
#
# The templates intentionally have separate database connection and pooling
# settings. Choose one before first start; both Docker Compose and local scripts
# read the resulting .env.
